# Summary

In summary, this book has no content whatsoever.

- Actual behavior

::: {#axm-specification}

## Axiom of specification

To every set $A$ and to every condition $S(x)$ corresponds a set $B$ whose elements are exactly those elements $x$ of $A$ for which holds $S(x)$.

:::

@axm-specification

- Expected behavior

::: {#@cnj-specification}

## Axiom of specification

To every set $A$ and to every condition $S(x)$ corresponds a set $B$ whose elements are exactly those elements $x$ of $A$ for which holds $S(x)$.

:::

@cnj-specification